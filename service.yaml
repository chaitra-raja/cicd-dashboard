apiVersion: v1
kind: Service
metadata:
  name: cicd-dashboard-service
spec:
  type: NodePort
  selector:
    app: cicd-dashboard
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080